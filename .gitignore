# Dependencies
node_modules/
.yarn/cache
.yarn/install-state.gz
.pnp.*

# Build outputs
.docusaurus/
build/
dist/

# Generated files
.docusaurus
.cache-loader

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Env / secrets (this is a PUBLIC repo — never commit secrets)
.env
.env.*
!.env.example
*.pem
*.key
.firebase/
.firebaserc.local
.npmrc

# Keystores / signing / service accounts (never tracked, even though docs-only)
*.jks
*.keystore
key.properties
keystore.properties
*service-account*.json

# Project-record / private notes
project-record-ignore/
*.ignore.*

# Lock-file from unwanted package managers (yarn-only repo)
package-lock.json
pnpm-lock.yaml
