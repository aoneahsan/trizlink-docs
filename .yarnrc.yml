nodeLinker: node-modules

enableGlobalCache: true

# Use the traditional node_modules layout instead of Plug'n'Play.
# Reason: Docusaurus 3.10 + modern Node hits ERR_INTERNAL_ASSERTION inside the
# PnP loader — switching to the node-modules linker sidesteps the issue cleanly.
